#include <stdio.h>

int main(){
    int len;
    printf ("Enter the value: ");
    scanf ("%d",&len);
    int arr[len];
    int sum=1;
    for (int i=0; i<len; i++);
    for (int j=0; j<len; j++)
        {
            printf("%d ",j);
   
        }
        return 0;
        }
